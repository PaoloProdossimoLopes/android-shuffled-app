<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Shuffled" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/turquoise_500</item>
        <item name="android:textColor">@color/black_800</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/gray_100</item>
        <item name="android:editTextColor">@color/black_800</item>
    </style>
    <style name="Theme.Shuffled" parent="Base.Theme.Shuffled" />
</resources>